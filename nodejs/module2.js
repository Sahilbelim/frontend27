var collection = {
     talk: function talk(name) {
          console.log(`${name} can talk `);
     }
     ,
     walk: function walk(name) {
          console.log(`${name} can walk `);
     }
     ,
     play: function play(name) {
          console.log(`${name} can play `);
     }
};

module.exports = collection;